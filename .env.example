# Copy to .env — never commit .env
# Use these names (not VITE_*) so secrets are not embedded in the client bundle.

AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
# Optional; omit to use app default table
# AIRTABLE_TABLE_ID=

TWELVE_DATA_API_KEY=

# Optional CoinGecko demo API key
# COINGECKO_API_KEY=
